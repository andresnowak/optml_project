# Baseline: AdamW on matrix parameters, with auxiliary AdamW for embeddings,
# biases, and norm gains. Aux params share scalar_weight_decay.
extends: gpt124m.yaml
matrix_optimizer: adamw
run_name: adamw
