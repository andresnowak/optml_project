# Random spectrum ablation: update singular vectors from M, normalized U(0,1) singular values.
extends: base.yaml
matrix_optimizer: dynmuon
routing_mode: fixed
fixed_p: 0.0
spectrum_mode: random_uniform
compute_mode: svd
run_name: random_spectrum
